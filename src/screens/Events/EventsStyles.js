import { StyleSheet } from 'react-native';

export default StyleSheet.create({
    container: {
        flex: 1,
        alignItems: 'center',
    },
    text: {
        fontSize: 35,
        fontWeight: 'bold',
        marginTop: 30,
        marginBottom: 25,
    },
});